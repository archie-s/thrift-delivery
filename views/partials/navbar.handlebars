<nav class="bg-gray-800">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between h-16">
            <div class="flex items-center">
                <a href="/" class="text-white font-bold text-xl">Thrift Delivery</a>
            </div>
            {{#if user}}
                <div class="flex items-center">
                    {{#if (eq user.role 'manager')}}
                        <a href="/manager/dashboard" class="text-gray-300 hover:text-white px-3 py-2">Dashboard</a>
                        <a href="/manager/riders" class="text-gray-300 hover:text-white px-3 py-2">Riders</a>
                        <a href="/manager/deliveries" class="text-gray-300 hover:text-white px-3 py-2">Deliveries</a>
                    {{else if (eq user.role 'rider')}}
                        <a href="/rider/dashboard" class="text-gray-300 hover:text-white px-3 py-2">My Deliveries</a>
                    {{/if}}
                    <a href="/auth/profile" class="text-gray-300 hover:text-white px-3 py-2">Profile</a>
                    <a href="/auth/logout" class="text-gray-300 hover:text-white px-3 py-2">Logout</a>
                </div>
            {{else}}
                <div>
                    <a href="/auth/login" class="text-gray-300 hover:text-white px-3 py-2">Login</a>
                </div>
            {{/if}}
        </div>
    </div>
</nav>
